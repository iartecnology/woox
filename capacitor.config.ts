import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.woox.app',
  appName: 'Woox',
  webDir: 'dist/woox-frontend/browser'
};

export default config;
